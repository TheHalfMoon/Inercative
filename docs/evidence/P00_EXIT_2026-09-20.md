# P00 Exit Evidence

**Date:** 2026-09-20  
**Canonical main baseline:** `629a2842e179ec6f26ec33d1c93537a9b915d5b0`  
**Status:** P00 COMPLETE / P01 READY

## Exit criteria mapping

### Toolchain baseline

Pinned repository commands and toolchain are committed and reproducible.

### CI baseline

The repository baseline runs on Ubuntu and Windows with frozen install, format, lint,
typecheck, and tests.

Fresh-main CI #101 on `629a2842e179ec6f26ec33d1c93537a9b915d5b0`: **SUCCESS** on Ubuntu and Windows.

### SpecGrain

SpecGrain is initialized and P00 work was shaped through the real pinned CLI. The current
CLI does not expose VERIFIED/CONTROLLED transitions for already-delivered Grains; no such
state is fabricated.

### Diffcipline

Diffcipline policy and an exact-diff proof are committed under SG-000002 / SG-000003.

### License and provenance

- Apache-2.0 project-owned source posture is canonical.
- Third-party notice policy is canonical.
- Donor import-record validation is canonical.
- Deterministic admission validation and notice inventory are canonical.
- No donor product code entered before the provenance gate.

### Alibaba Open Code Review

SG-000008 final candidate head:

`72f58ace0a37a426d36a7aeb6446dd08d1794351`

Repository CI #100:

- Ubuntu: PASS;
- Windows: PASS.

Evidence-only Alibaba OCR run `35522598679` used checksum-verified
`alibaba/open-code-review v1.12.7` Linux amd64:

`sha256:56649575421b8ca3be3cfaededc0a9037ae7cc9ae560f167bf504bfb4054451a`

Deterministic preview/accounting:

- changed files: 9;
- OCR-reviewable: 3;
- deterministically excluded: 6;
- rule resolution executed for all 3 reviewable files;
- all 6 excluded files received separate material review accounting;
- no unresolved material finding remained.

Semantic OCR truth state:

**NOT RUN / BLOCKED**

No scoped OCR/Anthropic endpoint variables were configured. `ocr review` failed closed
with the expected missing-endpoint error. This is explicitly allowed by the canonical P00
exit gate when the blocker is truthfully recorded; it is not represented as PASS and no
generic bot substitutes for OCR.

### Exact-head / fresh-main

PR #13 merged normally as:

`629a2842e179ec6f26ec33d1c93537a9b915d5b0`

Fresh-main CI #101: **SUCCESS**.

## P00 conclusion

Every P00 delivery gate is either executed with exact evidence or, for semantic OCR,
explicitly blocked under the canonical external-blocker exception. No unresolved material
review finding remains.

The next dependency-eligible task is:

`IN-P01-S01-T01 — Qualify control-plane framework`

P01 code implementation must not begin until that task is shaped into a new SpecGrain
Grain.
